# Implementation Log

Append-only. Each wave records: completed work, commands run, test results, known limitations, unverified integrations (SPEC §0 rule 7).

| Date | Wave | Agent | Summary | Tests | Unverified / Limitations |
|---|---|---|---|---|---|
| 2026-09-09 | 0 | F0 (foundation) | Installable `src`-layout package (hatchling, py3.12, uv.lock). pydantic-settings `Settings` (all SPEC §7 fields; placeholder-identity gate; SEC interval clamped up to ≥200 ms). SQLAlchemy 2 ORM for all 19 SPEC §9 tables (decimals as canonical TEXT via `decimal_to_text`/`text_to_decimal`; pinned unique keys on `fact_observations.observation_hash`, `normalized_facts` period identity, `chunks`, `embeddings`). Alembic initial migration `2679d5a12c9a_initial_schema` + programmatic `quarterline db upgrade`. `SecClient`: identity gate, thread-safe monotonic ≥200 ms request spacing (undershoot-proof loop), 429/503/conn-error backoff up to `SEC_MAX_RETRIES`, disk cache w/ ETag/Last-Modified revalidation + dated-cache fallback (SPEC §25). `fetch_companyfacts`/`iter_unit_facts`, `fetch_submissions`/`list_recent_filings`/`build_filing_url`, ordered `tagmap_us.yml` loader. FastAPI shell: `/` placeholder + footer disclaimer, security headers (nosniff, CSP self+inline), `/health` (DB SELECT 1; Ollama providers ≤0.5 s timeout, 30 s cache; 503 only on DB fail). `SUBCOMMAND_REGISTRY` CLI (db upgrade + serve live; others exit 2 with wave notices). Events stub (C12, JSONL). Watchlist CIKs verified against official `https://www.sec.gov/files/company_tickers.json` (script kept at `scripts/verify_watchlist_ciks.py`; that file 403'd once transiently — script has a data.sec.gov/submissions fallback that also verifies ticker+name). | `uv sync --extra dev` ok; `uv run alembic upgrade head` ok (stamps `alembic_version`, idempotent); `uv run pytest -q` → 45 passed (offline: MockTransport SEC tests, tmp-file migration tests, TestClient health tests, decimal/uniqueness ORM tests); `uv run ruff check .` + `uv run ruff format --check .` clean; `/health` via TestClient → 200 `{'status': 'ok', 'database': 'ok', 'generation_provider': 'ok', 'embedding_provider': 'ok'}` (Ollama running locally); `GET /` → 200 with disclaimer + nosniff/CSP headers; `quarterline ingest facts` → exit 2 wave-1 notice | Live SEC ingestion unverified (EDGAR_IDENTITY still placeholder — app refuses by design). `sentence-transformers` declared in `rerank` extra, not installed. PG profile, retrieval, generation, agent = later waves. SQLite treats NULL `period_start` as distinct in the `normalized_facts` unique key (instant facts); repos must upsert by full key. Alembic env.py must not run PRAGMAs on its connection (pysqlite rolls back the version stamp otherwise). AMZN = Consumer Discretionary (GICS), which the task brief's sector list omitted. |
| 2026-09-09 | 1 | F2 (documents) | Document pipeline per SPEC §13: `ingest/html_clean.py` (`html_clean-1`: content validation, EDGAR SGML `<DOCUMENT>` wrapper stripping, script/style/nav/hidden removal — scripts never executed or retained, inline XBRL `ix:*` unwraps to text with `ix:header` dropped, tables → readable `cell \| cell` rows, whitespace normalization, exact per-block `start_offset`/`end_offset` into cleaned text with roundtrip invariant); `ingest/sections.py` (`SectionCandidate{section_type,heading,start,end,confidence,notes}`; 10-Q Part I Item 2 / Part II Item 1A, 10-K Item 7 (never 7A) / Item 1A; ToC avoidance = largest-after-heading-body match wins, item/part state machine, title-keyword confirmation; uncertainty demoted to `other` @0.2 with note, short-body @0.5, missing sections stay missing — never a whole-filing mislabel; `earnings_release_candidate` = whole-exhibit section with signal score); `ingest/pdf_extract.py` (`pymupdf-1`: per-page offsets consistent in joined text, `ok/needs_ocr/failed`, <25 extractable chars → `needs_ocr`, corrupt → `failed` with hash retained); `store/repositories/documents.py` (`DocumentsRepo`: doc identity = company+accession+kind+source_url (exhibit filename), content-hash idempotency lookup, destructive `replace_sections`, offset/span resolution for later evidence spans, extraction-report counts; namespace-package import verified); `ingest/documents.py` (`documents-1`: watchlist discovery 4×10-Q + 2×10-K + 8-K Item 2.02 via submissions `items` array + `index.json` directory EX-99 name match (index files excluded), SecClient cache-first download → `source_artifacts` provenance (sha256, etag, last_modified, local_path, parser_version) → clean → sections → `documents`+`sections`; 8-K three-date logic `resolve_8k_dates` (filing date, event date = period-of-report, discussed period parsed ONLY from exhibit text "quarter/months/year ended <date>", else null + note — never inferred from filing date); `ingest_pdf_file` for IR PDFs (URL/company/date/page/hashes retained; needs_ocr → zero sections); `IngestReport.summary()`; `handle_ingest_documents` registered into `SUBCOMMAND_REGISTRY["ingest:documents"]` at import — no cli.py edit). Prices stub `sources/prices/{__init__,yfinance_provider}.py` (`YfinancePriceProvider.get_prices` raises NotImplementedError; wave F7 activates). Fixtures committed under `tests/fixtures/documents/` with `manifest.json`: REAL Apple 8-K EX-99.1 Q2 FY2026 (accession 0000320193-26-000011, 168,815 bytes, unmodified, sha256 on record) + synthetic `injection_filing.html` (labeled, stored as inert verbatim text) + synthetic PyMuPDF `synthetic_mda.pdf`; manifest records fetch UA + the Archives-403 adaptation (added contact suffix to the plan UA after www.sec.gov rejected the bare form; data.sec.gov accepted it). 41 tests (html_clean 12, sections 13, pdf_extract 8, repo 5, pipeline e2e 7: end-to-end tmp-DB, idempotent re-run no duplicates, 3-date fabricated-payload unit, CLI handler via `cli.main`, injection stored inert, PDF provenance, needs_ocr not indexed). | `uv run pytest -q tests/unit/test_html_clean.py tests/unit/test_sections.py tests/unit/test_pdf_extract.py tests/unit/test_documents_repo.py tests/integration/test_documents_pipeline.py` → 41 passed; full suite excluding F1's concurrently in-flight files → 86 passed; `uv run ruff check` (my scope) clean; `ruff format` applied to my 12 files only. Pipeline-over-fixtures into tmp DB printed: 3 documents (10-Q ok mda[169,469)+risk_factors[500,739); real 8-K exhibit ok earnings_release[0,11342) period_end=2026-03-28 ≠ filed 2026-04-30; injection exhibit ok, discussed 2026-06-30) + text PDF ok (page section, 1047 chars) + image-only scan → needs_ocr, 0 sections | Unverified/limitations: live SEC discovery+download through `SecClient` unexercised end-to-end (identity gate; EDGAR_IDENTITY still placeholder) — pipeline verified offline via fixture-backed fake transport; `quarterline ingest documents` standalone still prints the W0 wave notice because nothing in the console-script path imports `quarterline.ingest.documents` (handler IS registered; needs a one-line import in `ingest/__init__.py` or orchestrator-owned `ingest/pipeline.py` — both outside my file list); W0 `documents` schema has no event_date/notes column → 8-K event date + determination note ride on `IngestReport.eight_k_dates`, `period_end` carries the discussed period (recommend migration); W0 `sections` schema has no confidence/notes columns → carried on `SectionCandidate`, surfaced in `IngestReport.low_confidence_sections`; documents table has no DB unique constraint on the identity key — enforced in `DocumentsRepo.upsert_document` only; F1's concurrent test files (`test_normalization/test_periods/test_quarter_label/test_ratios/test_quality/test_facts_*`) were failing/erroring during my runs (their churn also trips W0 `test_cli_events::test_unimplemented_subcommands_exit_2_with_notice` once `ingest:facts` registers — orchestrator will need to update that W0 test at wave boundaries); project-wide `ruff format .`/`ruff check .` gated by F1 files, not mine |
| 2026-09-09 | 1 | F1 (financial) | Financial normalization end-to-end (SPEC §10–§12, Milestone 2). `core/periods.py`: duration-band period-kind inference (13/14-week quarters, 52/53-week years; no 90-day assumption), FYE-month fiscal math with 53-week spill handling, fiscal anchors, quarter-sequence helpers, `QuarterPeriod`. `core/normalization.py`: companyfacts→observations (raw fy/fp kept in `context_metadata_json`, sha256 `observation_hash` over company+tag+unit+periods+value+frame → re-ingestion idempotent, revised values preserved alongside originals); selection groups by economic-period identity then applies tag priority AFTER unit/scope filtering (annual can never displace quarterly); YTD→quarter derivation (Q2=6m−Q1, Q3=9m−6m, Q4=annual−9m) with all §10.3 conditions incl. contiguity + revision-vintage window (vintage attaches to the value's FIRST filing, so unchanged comparatives don't manufacture mismatches); EPS/weighted-shares/instants never derived; missing concepts logged never filled; `as_of` policy excludes filings after timestamp. `core/ratios.py`: all §11 ratios in Decimal via MetricValue {value,unit,status,input refs,formula_version}; capex preserved-then-normalized sign semantics w/ unexpected-sign review flags (no blind abs()); growth null for non-positive priors + turned-profitable/loss-making notes; margin changes in pp; cfo_to_net_income `unsuitable` when NI≤0. `core/quarter_label.py`: §12.1 five-signal label with exact precedence (2-quarter cfo_ni<0.4 veto first, <3 available → insufficient data, ≥3 true/false, else mixed), every input+rule exposed; §12.2 experimental fundamental score with scale/clamp, weight rescaling, no aggregate below 60% weight. `core/quality.py`: per-quarter coverage (direct/derived/missing fraction). `core/provenance.py`: `build_fact_card(s)` (latest 8 quarters + latest annual) with observation-id lineage or derived-from + formula_version on every MetricValue; `explain_metric` ProvenanceReport (accession/form/filed_at, direct vs derived vs computed, formula text). `store/repositories/{base,companies,facts}.py`: typed session-scoped repos; hash insert-or-skip observations; NULL-`period_start`-safe full-key upserts (SQLite NULL-in-unique-key gotcha); lineage roles direct_source/annual_total/prior_ytd; idempotent derived-metric upserts; `quarters_available`/`facts_for_quarter`/`metric_series`/as-of variants (as-of reconstructs direct + derived facts from observations/lineage at the timestamp). `ingest/facts.py`: `ingest_facts` pipeline (watchlist → SecClient cache-first fetch → observations → normalize → derive → derived metrics; per-company error isolation; idempotent re-run) + `verify facts` table; CLI handlers registered via `register_subcommand` at module import. Fixtures: real trimmed AAPL companyfacts (fetch 2026-09-09, UA "Quarterline build (fixture verification)", tagmap tags only, period end ≥ 2023-06-01, 400 verbatim entries, sha256 in manifest) + clearly-marked synthetic edge-case fixture (Oct FYE, 53-week year, restatement, unit-incompatible tag, EPS-never-derived) + provenance manifest. Tests: 147 new (83 unit + 64 integration incl. full AAPL-fixture pipeline, restatement as_of, idempotency, CLI) — all §26 financial+score lists covered. | `uv run pytest -q` → 192 passed (45 W0 + 147 new); `uv run ruff check .` + `uv run ruff format --check .` clean; `verify facts` through SUBCOMMAND_REGISTRY exit 0 with per-concept 8-quarter provenance table; re-ingestion second run: 0 observations inserted, 0 facts created; demo: AAPL latest-4-quarter revenue/net_income/cfo all reconcile with fixture values, Q4s derived from annual−9m with lineage. | CLI WIRING GAP: `quarterline/cli.py` imports nothing from ingest (by W0 design, file not owned by F1) so `uv run python -m quarterline ingest facts / verify facts` still exits 2 until one line is added to `src/quarterline/ingest/__init__.py`: `import quarterline.ingest.facts  # noqa: F401`. Handlers themselves are verified through the registry. Conservative revision-vintage window (≤100 days between input filings) can refuse derivations where inputs are filed >1 quarter apart; W0 wave-notice unit test (`tests/unit/test_cli_events.py::test_unimplemented_subcommands_exit_2_with_notice`) still expects `ingest facts` unimplemented — my integration tests guard/clean the registry so the full suite stays green, but the orchestrator may want to update that W0 assertion at the wave boundary. Live SEC ingestion still unverified (EDGAR_IDENTITY placeholder); fixture fetch used the declared build UA. |
| 2026-09-09 | 2 | F4 (UI/API deterministic) | All SPEC §19 non-LLM routes, deterministic over the store, zero LLM imports. `routers/companies.py`: watchlist `GET /` (15 rows from the DB, AAPL fixture metrics + "no data ingested" graceful rows with no zero substitution, HTMX partial swap on `HX-Request`), company page `GET /c/{ticker}` (?period_end quarter picker, 8-quarter Chart.js charts fed by an inline `#chart-data` JSON payload + always-rendered summary table as the no-JS fallback, per-quarter fact table with direct/derived(subtraction)/derived(formula)/missing badges, FORMULA_TEXT + formula_version per row, warnings column, SPEC §12.1 label panel showing all five signals (id/input/state/detail), the applied rule and the exact caption, brief placeholder panel "available in a later release"), provenance page + JSON `GET /c/{ticker}/provenance/{metric_id}?period_end=` (METRIC_IDS allowlist gate -> 404; C6 `explain_metric`; display-level enrichment restores growth-metric lineage because core `derived_from` refs are `revenue_yoy:current`-style and unresolvable by `explain_metric` — upstream fix belongs in core, see limitations). JSON APIs: `/api/c/{ticker}/facts[?period_end=]` (fact card), `/api/c/{ticker}/metrics` (8 quarters); all numbers serialized via pydantic `model_dump(mode="json")` (Decimal -> canonical string, never float); unknown ticker/period/metric -> 404 `{"error": ...}`. Decimal-exact display helpers ("n/a"/"missing", never "0") exported for reuse. `routers/screener.py`: exact filter set `revenue_yoy_gt`/`operating_margin_gt`/`operating_margin_change_pp_gt`/`fcf_positive`/`quarter_label`/`sector`/`minimum_data_coverage`, GET + POST (urlencoded body parsed directly — python-multipart not a dependency), every submitted value echoed, explicit empty state, HTMX partial support, invalid decimals -> visible per-field errors with the filter dropped (never coerced). `routers/filings.py`: filing viewer `GET /filings/{document_id}` (metadata incl. the 8-K dates + hardened rel=noopener-noreferrer sec.gov link; ONLY cleaned `sections` text, Jinja autoescape on, never the raw `\|safe`-style injection of filing HTML per SPEC §2.3.7) and `GET /evidence/{evidence_id}` per contract C8 (sha1 over document_id+strategy+start+end+text_hash resolved against `chunks`; explicit "evidence not found (index not built)" 404 until wave F3 populates it; finalized at integration). `main.py`: router registration only (placeholder `/` moved into `routers.companies`; CSP untouched). `base.html`: nav Watchlist/Screener/Dashboard (dashboard 404s gracefully until wave F6) + research-only + label disclaimer footer on every page; vendored htmx 1.9.12 (0-Clause BSD) + Chart.js 4.4.3 (MIT) downloaded into `static/vendor/` with `LICENSES.md` provenance record (page works fully offline; if Chart.js fails to load, `app.js` degrades to the server-rendered tables). `static/app.css` (tables, badges, chips, form grid) + `static/app.js` (charts from the Decimal-string JSON payload; Number conversion only for plotting). Tests: 45 new — `tests/api/test_api_json.py` (8-quarter series, recursive no-float assert over the JSON, Q4 missing EPS, 404 JSON matrix, provenance contract, LLM-modules-blocked via sys.modules=None) and `tests/integration/test_ui_{watchlist,company,screener,filings}.py` (15-row watchlist + 14 graceful empty rows + no-zero-substitution, HTMX partial, disclaimer on every page parametrized, security headers, label panel signals, missing-badge rows, quarter picker, hand-derived screener expectations read from the fixture (YoY = Q3-2026 rev / Q3-2025 rev − 1), combined/label/sector/coverage filters + validation errors + POST echo, escaped <script> and injection-fixture text rendered inert, evidence C8 resolve + not-built 404), with `tests/ui_test_helpers.py` (reuses `facts_test_helpers` fixture pipeline; `facts_cli_guard` keeps the W0 CLI-registry test green) and `scripts/ui_smoke.py` (boot check). | `uv run pytest -q tests/api tests/integration/test_ui_watchlist.py tests/integration/test_ui_company.py tests/integration/test_ui_screener.py tests/integration/test_ui_filings.py` -> 45 passed. `uv run pytest -q` -> 236 passed, 1 failed (pre-existing wave conflict, not F4: `tests/unit/test_cli_events.py::test_unimplemented_subcommands_exit_2_with_notice` now executes wave-F3's real `index:build` handler via the cli `_WAVE_PACKAGES` lazy import — the same wave-boundary pattern F1 logged; F3's files also transiently broke `ruff check` mid-edit). `uv run ruff check` on the F4 scope (src/quarterline/api, scripts/ui_smoke.py, my test files) -> clean; `ruff format` applied to my 10 py files only. `uv run python scripts/ui_smoke.py` -> fixture ingest errors: none; 21 routes exercised with OLLAMA at a dead port: / 200, /screener 200 (GET + POST + filters), /c/AAPL 200 (+period_end variant), /c/MSFT 200 (graceful empty), /c/NOPE 404, provenance page+JSON 200, bogus metric 404, facts/metrics JSON 200, unknown ticker 404, /filings/{id} 200, /filings/999999 404, /evidence/{resolved-id} 200, /evidence/ev-0123456789ab 404 (index not built), /health 200; smoke: OK. | Unverified / limitations: (1) CORE GAP for orchestrator/F1: `core/provenance.explain_metric` cannot resolve input lineage for growth-style metrics (`revenue_yoy`, `shares_yoy`, `operating_margin_change_pp`) because their `derived_from` refs are `<metric_id>:current` / `:prior_year_quarter` rather than concept names, so `input_concepts` parses to the metric's own name and `sources` returns empty; F4 works around it with a display-level enrichment map (`routers/companies.py::_enrich_growth_sources`, mirrors FORMULA_TEXT) — the authoritative fix belongs in core, after which the enrichment stays a no-op (it fires only when sources are empty). (2) `/evidence/{id}` scans `chunks` linearly and is finalized at integration with F3 (chunks were empty in my tests; the seeded-chunk contract test proves the C8 id scheme). (3) `/dashboard` nav link 404s gracefully until wave F6. (4) Screener POST parses urlencoded bodies directly because python-multipart is not a project dependency; multipart form posts are unsupported (browser default and HTMX are urlencoded). (5) Invalid screener input -> visible error with that filter dropped (never silently coerced). (6) Live ingestion still gated on EDGAR_IDENTITY (unchanged); the UI is verified only over committed fixtures. |
| 2026-09-09 | 2 | F3 (retrieval) | Retrieval track per SPEC §14–§16 (SQLite profile, PLAN C7/C8/C9). `retrieve/chunk_fixed.py` (`fixed-1`: ~400-token windows, ~80-token overlap, paragraph-boundary preference via blank-line blocks, oversized paragraphs split at sentence bounds then word-window hard cap ≤460; single documented estimator `estimate_tokens` = ceil(chars/4), labeled approximate; shared block/packing engine with guaranteed-progress overlap backtracking + fully-contained-span suppression); `retrieve/chunk_section.py` (`section-1`: children ~200 tok/40 overlap, parent = full section stored, query-time expansion pre-materialized as bounded ≤560-token `window` rows so every supplied window stays C8-resolvable without truncation — 560 chosen ≤ §16's 600-token passage cap; task's ~1200 example deviated, documented). Row-family convention: `strategy_version` suffixes `-parent`/`-window`; only base rows embedded/FTS-indexed/retrievable; identical-text drafts skipped (tiny sections collapse to one child). `retrieve/embeddings.py` (C9 protocol; `OllamaEmbeddingProvider` batched `/api/embed`, availability check via `/api/tags`, Nomic `search_document:`/`search_query:` prefixes applied by callers, normalize flag, dim learned on first embed; `FakeEmbeddingProvider` deterministic hash vectors, NON-PRODUCTION, `EMBED_PROVIDER=fake` for offline CLI/tests; `EmbeddingProviderUnavailable`, `EmbeddingModelMismatchError`). `store/repositories/search_sqlite.py` (`SearchIndexRepo`: idempotent chunk writes via unique key (rebuild ⇒ identical rows/ids), contentful FTS5 `chunks_fts(text, chunk_id UNINDEXED)` kept in sync per document, float32-LE vector BLOBs, embeddings cache lookup by (text_hash, provider, model, revision) reused ACROSS strategies, C8 `get_chunk_by_evidence_id` (content-derived scan; persisted evidence_id column would need a migration — reported), child→window map, `index_manifest` table (raw-SQL schema outside W0 ORM, migration recommended): deterministic `index_version` = sha256(provider|model|revision|strategy|strategy_version|corpus_text_hash_set)[:16], per-(strategy,version,model) upsert; corpus stats). `retrieve/lexical.py` (FTS5 joined to metadata filters, one fixed-fragment arm per strategy UNION ALL'd, expanding lists as explicit per-item bound params, `ORDER BY bm25() ASC` with documented more-negative-better sign + rank exposure; `sanitize_fts_query` quotes every word token — operators/NEAR/colons/punctuation dropped, injection-safe). `retrieve/vector.py` (metadata filter FIRST in SQL, then NumPy cosine over BLOB vectors, dim guard, deterministic ties). `retrieve/fusion.py` (RRF Σ1/(60+rank), ranks only, deterministic tie-break, top 15). `retrieve/reranker.py` (`CrossEncoderReranker` import-guards `sentence_transformers` ([rerank] extra, not installed), lazy load w/ cached failure reason, `maybe_rerank` never raises, never fakes scores). `retrieve/search.py` (`SearchService` §16 flow: mode section presets brief={mda,earnings_release} risk={risk_factors,mda} general=all, explicit sections override mode; lexical 20 + dense 20 → RRF 15 → optional rerank → top_k; hybrid degrades to lexical on provider unavailability (§25); model-mismatch gate raises `EmbeddingModelMismatchError` for dense/hybrid when the strategy's manifest was built with a different model; `evidence-policy-v1` deterministic abstention (≥2 items hybrid / rank≤5 lexical / cosine≥0.20 dense anchor + ≥1 query-term overlap — explicitly NOT calibrated probabilities); `build_index` per-section chunking with offset shifting into document coordinates; run events via C12 stub; `index:build`+`search` CLI handlers registered at package import, `EMBED_PROVIDER=fake` supported offline). `retrieve/context.py` (≤6 passages, ≤600 tok each, 10% estimator headroom on the budget, same-document overlap dedup, second-source diversity for passage 2, oversized windows skipped whole — never truncated so evidence ids stay resolvable; budget = MAX_PROMPT_TOKENS−MAX_OUTPUT_TOKENS−headroom). Fixtures: `tests/fixtures/retrieval/{generate_fixtures.py,fixture_embeddings.json}` — 17 chunk + 5 query vectors from FakeEmbeddingProvider(seed 20260909, dim 64) over the offline fixture corpus (real 8-K + synthetic 10-Q/injection/PDF), regeneration byte-identical (sha256-verified twice), manifest documents provenance ("NOT real model output — deterministic test artifact") + regen command; `tests/retrieval_test_helpers.py` corpus fixture. Tests: 57 new (43 unit + 14 integration incl. CLI) covering the §26 retrieval list verbatim: correct-company (lexical+dense+hybrid), correct-period (disjoint docs per discussed period), form filter, FTS5 ordering w/ documented sign, RRF recomputed from source ranks (+hand-computed unit case), embedding-model mismatch rejection (lexical survives), bounded parent expansion (≤560), context budget, evidence-ID C8 roundtrip + offset-slice invariant vs document text, reranker failure fallback (degraded metadata, no fake scores), rebuild idempotence (identical chunk ids) + strategy coexistence (fixed/section/parent/window families), mode presets + explicit override, insufficient evidence (empty corpus/no-match) + sufficient pass, index_version changes with model, fixture-vs-provider vector regression. Wave-boundary update (same as orchestrator's wave-1 edit): `tests/unit/test_cli_events.py` wave-notice assertions switched from `index build`/`search` (now implemented, handlers registered from `quarterline.retrieve`) to the still-stubbed wave-3 `eval` subcommands. | `uv run pytest -q` → 309 passed (192 before + 117 new incl. retrieval); `uv run ruff check .` → All checks passed; `ruff format --check .` → 108 files already formatted (formatted only my 17 files); `uv run python -m quarterline index build --strategy fixed --help` → usage, exit 0; `search --help` shows --ticker/--query/--strategy/--retrieval; e2e demo over the REAL fixture corpus: ingest (3 SEC docs + PDF) → `index build` fixed (11 chunks, 11 emb, version 7d91c9b81fb2797f) + section (6 children+3 parents/10 windows, version 783028b11161d7e1) → `search --ticker AAPL --strategy section --retrieval hybrid "Apple revenue March quarter results"` ranks the real EX-99.1 window ev-0f0259cc182a #1 with lexical+dense+rrf scores; hybrid-rerank degrades with `degraded[reranker]` and keeps fused order; unknown ticker → `INSUFFICIENT EVIDENCE (evidence-policy-v1)`; run events JSONL per search with corpus_version/latency/status | Limitations/unverified: Ollama embedding path verified only via httpx.MockTransport request-shape tests (Ollama not exercised live — `nomic-embed-text` not pulled; live path is code-complete); PG/pgvector profile is wave F8 scope; W0 `chunks` schema has no role/evidence-id column → row-family encoded via `strategy_version` suffix and evidence ids resolved by content-derived scan (both documented, migration recommended); full cleaned document text is not persisted by the W0 pipeline, so the index covers extracted sections only (un-sectioned interstitial text is not searchable — indexer chunks per section; acceptable because sections carry the evidence, reported for orchestrator awareness); W0 search parser has no `--mode`/`--top-k`/`--tickers` flags → handler reads `getattr(args,...)`/`QUARTERLINE_SEARCH_MODE` env (gap for orchestrator's parser, one line each); F4's concurrent API/UI files were excluded from my runs — project `ruff check .` was clean at my final run; query-embedding caching not implemented (query vectors recomputed per search — chunk cache makes indexing cheap; queries have no chunk row to key on). |
| 2026-09-09 | 3 | F5 (grounded generation) | Grounded generation per SPEC §17–§18/§2.2 (PLAN contracts C10/C11). `llm/base.py` (C10 `GenerationProvider` protocol + `GenerationResult`{text, provider, model, input/output_tokens, token_count_source: provider\|estimate, latency_ms, finish_reason}; `GenerationProviderUnavailable`, `RemoteFallbackNotConsented`; documented chars/4 token estimator shared with retrieval). `llm/ollama.py` (`/api/chat`, `options.num_ctx` from settings, per-call `GET /api/tags` model verification → unavailable if missing (SPEC §4), provider usage when reported else explicit `estimate`, done_reason mapping, latency; MockTransport-tested only). `llm/openrouter.py` — HARD consent gate: constructor requires ALLOW_REMOTE_FALLBACK=true AND non-empty key AND model, else `RemoteFallbackNotConsented`; chat/completions with Bearer auth + response_format json_object. `llm/schemas.py` (C11): `MetricMention{metric_id(allowlist-validated against core.METRIC_IDS), template(reported_change\|reported_value\|reported_level)}`, `BriefBullet`/`BriefRisks`/`OpenQuestion{text,evidence_ids}`, `Brief{status ok\|partial\|insufficient_evidence\|provider_unavailable\|refused, label_echo, ...}`, `Answer{text,evidence_ids,status}`; extra="forbid" everywhere; brief/answer JSON-schema descriptions for providers. `llm/prompts.py` + `prompts/{brief,qa,memo}/v1.txt`: versioned (dir/file), sha256-hashed artifacts (cache-key + run-event components), STRICT `{placeholder}` render (missing/unused var = error); system prompts state every §17 prompt rule verbatim incl. "numbers must NEVER be typed — use metric_mentions", passages-as-evidence-not-instructions, §2.2 citation rules, JSON-only, research-only. `llm/repair.py`: generate once → if JSON or pydantic-invalid, exactly ONE repair pass (invalid output + schema sent back) → else controlled `GenerationFailure`; `RepairOutcome` records first_pass_valid/repair_used (feeds §22 metrics). `core/citations.py` (§18 checks 4–7): evidence map of SUPPLIED windows (EvidenceRef: document_id, ticker, period from documents row); existence, declared↔in-text membership, company identity, period compatibility (unknown period ⇒ rejected: cannot validate reliably), sentence-level `[ev-…]` format (citation must close the sentence); whole-statement drop with per-check reasons. `core/factcheck.py`: (a) metric-mention EXPANSION — Python renders numbers Decimal-exact from the card (USD→$x.x billion/million, EPS, shares, %, pp, ratio x, score; documented level-wording thresholds), structural template↔metric-shape table (reported_change only for *_yoy/*_change_pp etc.), missing metric ⇒ dropped with issue; (b) free-text numeric checking: documented normalizer ($ € £, thousands separators, k/m/b + thousand/million/billion words, % vs pp, minus + accounting parens incl. `$(2.0)` normalization), metadata masking (evidence ids, ISO dates, month-day-year, FYyyyy, bare years, 10-K/Item/EX-, accession-like — accession mask ordered BEFORE ISO mask), SEMANTIC matching first (unit class + sign + scale; zero only matches zero; same digits elsewhere insufficient; QA answers carry an empty card ⇒ any typed number unsupported), ≤0.5% relative tolerance only after semantic match plus display-rounding respect (1/2-dp displays). `core/advice_policy.py` (`advice-policy-v1`, no LLM): advice-SEEKING request detector (buy/sell/hold near investment context, should-I-invest, good/best stock, priced to buy, price target, worth buying, undervalued, recommend holding…) and advice-ISSUING content detector (you-should, we-recommend, is-a-buy, price target of, guaranteed returns); research-only refusal text with alternatives + §12.1 caption. `llm/generation.py`: pipeline = fact card (label) → SearchService (mode presets, company + period filters, hybrid w/ lexical fallback on embedding-model mismatch per §25) → bounded context (≤6 windows) → insufficient-evidence policy ABSTAINS without provider call → brief_cache lookup keyed sha256(company, period, fact-card hash excl. generated_at, evidence/context hash, provider, model, prompt version+hash, generation settings, validation/factcheck/advice versions) → ONE provider call ([system, user JSON payload: allowlist + metric catalog (id/unit/status, no values) + evidence]) → repair → VALIDATION GATE `validate_generation` checks 1–10 in order (schema, status/abstention, label_echo EQUALITY vs code label, citations 4–7, metric-reference validity, numeric consistency, advice compliance) with `ValidationReport` (per-check reasons, retained/dropped counts, version `validation-v1`); survival: drops⇒partial, nothing useful⇒insufficient_evidence, advice-driven loss⇒refused, provider down⇒provider_unavailable (facts+evidence shown, never fake prose), invalid-after-repair⇒controlled failure; mention expansion rendered BEFORE assembly; cache persist successes-only w/ expiry; C12 events (prompt version/hash, tokens+source, latencies, json_valid(+first_pass), repair_used, citation_valid, factcheck_passed, retained/dropped bullets, answer_status, cache_hit, error_type, estimated_api_cost=null). `answer_question`: advice check FIRST (refusal without provider call), no-ticker ⇒ explicit company-scope notice, citation+number+advice gated. API: `POST /api/c/{ticker}/brief` (JSON or urlencoded body {period_end?}; HTMX/Accept → `partials/brief.html` partial, else JSON; 400 unknown fields/bad date, 404 unknown ticker/period) and `POST /api/ask` (JSON {question, ticker?, mode}, pydantic extra-forbid; partial `partials/ask.html`); LLM imports are LAZY inside handlers so deterministic routes never import the stack (F4 block_llm_modules tests stay green); explicit degraded banner "generation unavailable — showing facts and evidence, not generated prose". `memo_review.html` static shell (wave-4 header). main.py: only the F5 marker line replaced (+ import line); F6 marker untouched. Tests: 87 new (`tests/generation_test_helpers.py` seeds facts fixture + period-ALIGNED document corpus + section index offline, ScriptedProvider records calls; unit: schemas/prompts(§17 rules verbatim)/providers(MockTransport: tags-miss, request shape+num_ctx+format, estimate-vs-provider, consent gate matrix)/repair(2-call guarantee, fenced JSON, controlled failure)/citations(7 cases)/factcheck(18: normalization incl. $(2.0), k/m/b, pp-vs-%, metadata masking, semantic+tolerance+sign+zero, Decimal-exact expansion)/advice/ validation-gate(§26 list incl. invented+wrong-period citations, label mismatch, unsupported 44.7%, structural wrong-metric, partial survival, refusal); integration: pipeline (ok brief w/ rendered numbers + resolvable citations + misaligned-doc exclusion, single repair e2e w/ event assertions, cache hit zero provider calls, provider-down degradation, abstention zero calls, ask refusal/insufficient/grounded) + API (degraded JSON+HTML banner, advice refusal, 404/400s, GET 405, success partial via injected provider). | `uv run pytest -q` → 496 passed (309 before; includes F6's concurrent additions); `uv run ruff check .` → All checks passed; `ruff format` applied to my 24 files only (project-wide format left to orchestrator due to concurrent waves); TestClient verification (real output in report): POST /api/c/AAPL/brief w/ Ollama dead → 200 {status: provider_unavailable, 26 fact rows, 2 evidence windows, brief: null, reason "ollama unreachable…"}; POST /api/ask advice → 200 {status: refused, advice-policy-v1, research-only text}; HX brief → banner "generation unavailable … not generated prose" present; `quarterline eval generation` (F6 harness) runs over my pipeline exit 0. | Limitations/unverified: no live Ollama/model calls anywhere (offline rule) — provider paths verified via httpx.MockTransport only; free-text numeric matching cannot verify CONCEPT identity from prose (documented; the metric_mentions channel i
| 2026-09-09 | 3 | F6 (eval + observability) | Evaluation + observability track per SPEC §21–§24. `eval/dataset.py`: pydantic §21 loader/validator for `data/eval/questions.jsonl` (unique ids; reviewed mandatory; answerable ⇔ expected_behavior=answer with ≥1 gold span; the three abstention kinds modeled distinctly — `insufficient_evidence` / policy `refusal` / provider failure is an outcome, never a dataset expectation); `document_id` = stable EDGAR accession string (dashed) resolved via `documents.accession` (dash-stripped), spans stay chunking-independent; optional per-span `anchor` makes `reviewed` machine-checkable via `verify_spans` (section-local slice under the pipeline's roundtrip invariant). `data/eval/questions.jsonl`: 11 reviewed questions over the REAL frozen Apple EX-99.1 Q2 FY2026 exhibit (accession 0000320193-26-000011) covering all §21 categories: 2 management-explanation (CEO revenue-driver quote [447,1055), CFO cash-flow quote [1057,1487)), period-specific diluted EPS [292,445), capital allocation dividend/$100B buyback [1489,1861), risks safe-harbor [2792,3800), cross-period total net sales 111,184 vs 95,359 [5864,5922), H1 operating cash flow 82,627/53,887 [10195,10251), Q1-FY26 period trap (period_end 2025-12-27 → filtered empty → insufficient), retail-store-count unanswerable, MSFT wrong-company trap (no MSFT docs), buy-advice refusal. Offsets verified in-session by running `clean_html` on the committed fixture and asserting each anchor ∈ text[start:end] (receipts also asserted in tests). `eval/retrieval.py`: precise §22 metrics — Hit@5 (any gold-overlapping unit in top-5), Recall@5 (OVERLAP-COUNT mean |gold covered|/|gold| — unit tests include a case where Hit=1.0 while Recall=0.5), MRR (first relevant reciprocal rank), wrong-company rate, period-filter error rate (+unknown bucket), latency P50/P95 (nearest-rank, from `SearchService` wall timings), `run_matrix()` = 2 strategies × 4 configs; `ensure_fixture_corpus()` mirrors `tests/retrieval_test_helpers` (same canned client, same doc ids, idempotent) for offline CLI runs. `eval/generation.py`: harness against a `GenerationRunner` PROTOCOL (never F5 internals) measuring JSON validity before/after repair, citation validity, numeric-error rate pre-filter, unsupported-content remaining (deterministic unbacked-number proxy), bullet retention, empty answers, answer relevancy (documented PROXY: question-term overlap + concept coverage), correct abstention, refusal accuracy (with research-only alternative), provider-failure rate — distinct status buckets; §24 events per run; eval_runs/eval_results persistence; `ScriptedFakeGenerationRunner` labeled NON-MEASUREMENT. `eval/judge.py` + `prompts/judge/v1.txt`: pinned rubric prompt (faithfulness/relevance 1–5 + rationale JSON), enabled only with ENABLE_LLM_JUDGE+JUDGE_PROVIDER+JUDGE_MODEL, injectable transport, full provenance (provider/model/prompt version+hash/raw/parsed/human_reviewed=False), labeled "never ground truth". `eval/regression.py`: committed baseline `data/eval/baselines/fixture_baseline.json`; thresholds Recall@5 drop >5pp FAIL (exactly 5pp passes, 6 fails — unit-tested), wrong-company ANY increase FAIL, new-config FAIL (when a baseline exists); `no_baseline` status informational; writes require `confirm=True` / CLI env `QUARTERLINE_EVAL_WRITE_BASELINE=1` + printed diff (never silent — the `--write-baseline` CLI flag is impossible without editing cli.py, env-var gate documented as the getattr-style escape hatch). `eval/report.py`: markdown+JSON into `storage/eval_reports/` with timestamps, dataset version, sample sizes, 2×4 matrix table, fixture-only labels; `latest_report()` feeds the dashboard. `observability/events.py` rewritten keeping C12 signatures: JSONL mirror + best-effort persistence upserting `runs` (status escalation ok<degraded<error) + appending `run_events`; `question`/`query` replaced by sha256 hash + length (never stored raw), secret-looking keys redacted; DB failures swallowed (telemetry never breaks the caller). `observability/metrics.py`: request counts, P50/P95 per latency kind computed in Python, provider/model breakdown, cache-hit, JSON repair, numeric-rejection, bullet retention, insufficient-evidence/refusal/provider-failure rates — every rate with its n. `observability/tracing.py`: `run_trace`/`recent_runs`. API: `routers/runs.py` (`GET /api/runs`, `GET /api/runs/{id}` timeline, 404) and `routers/dashboard.py` (`GET /dashboard`: metrics, latest eval report/eval_runs summary, recent runs + latest-run trace partial, empty state, sample-size disclaimer) registered at the F6 marker in `api/main.py` (F5's marker untouched); `templates/dashboard.html` + `partials/{dashboard,run_trace}.html` (nav entry exposed inside dashboard.html only — base.html is F4's; orchestrator should drop the "Arrives in a later release" title attr). `.github/workflows/scheduled_eval.yml`: workflow_dispatch + weekly cron, offline fixture eval + regression check (exit 1 on FAIL), report artifacts, self-hosted/opt-in real-model note ("CI never pretends a fake provider measures generation quality"). Tests: 65 new (unit: dataset validation incl. duplicate-id/unreviewed/no-span rejections + committed-dataset category coverage; hand-computed Hit/Recall/MRR/wrong-company cases; regression 5pp/6pp/wrong-co+1; baseline confirm-gate + diff; repair/JSON; judge disabled-by-default + mock transport + out-of-scale rejection; report markdown; percentile; observability: JSONL+DB persistence, upsert-once, hash-and-length-only sanitation incl. JSONL side, redaction, missing-schema survival, metrics aggregation/empty store, trace ordering; integration: every gold anchor resolves in ingested text, 2×4 matrix shape, frozen fixture retrieval levels, wrong-company/period traps, generation metrics incl. failure-class distinctness, eval rows persisted, runs API + dashboard rendered/empty states). Wave-boundary update: `tests/unit/test_cli_events.py` eval wave-notice assertions → registration assertions (same pattern as waves 1–2). | `uv run pytest -q` → 496 passed, 0 failed (full suite incl. concurrent F5 files); `uv run ruff check .` → All checks passed; `uv run ruff format .` → all formatted; `uv run python -m quarterline eval retrieval --dataset data/eval/questions.jsonl` → exit 0, measured fixture matrix: fixed/lexical Hit@5 71.4% Recall@5 71.4% MRR 0.714; fixed/dense 57.1/57.1/0.350; fixed/hybrid 71.4/71.4/0.548; fixed/hybrid-rerank 71.4/71.4/0.548 (degraded); section/lexical 57.1/57.1/0.571; section/dense 57.1/57.1/0.405; section/hybrid 57.1/57.1/0.429; section/hybrid-rerank 57.1/57.1/0.429 (degraded); wrong-co 0%, period-err 0% everywhere, n=11, latencies ~5–8ms p50; regression vs committed baseline PASS. `eval generation` → scripted-fake harness check (JSON 1.0, citations 7/7, abstention 3/3, refusal 1/1, prov-failure 0). `GET /dashboard` + runs API exercised via TestClient against the dev store (50 runs, timeline JSON, 404 path, empty state). | GAP (F3 file, not fixed by F6): `_pack_chunks` in `retrieve/chunk_fixed.py` stops early on the real exhibit — fixed covers 0–5639 of 11342 chars and section emits only child 0 (0–445)+window, so the financial-statement tables (gold spans at 5864 and 10195) are unreachable by BOTH strategies; this is why Hit@5/Recall@5 cap at 5/7 (fixed) and 4/7 (section) on the fixture corpus — the frozen tests pin today's levels so a fix can only raise them. Baseline is fixture-corpus-only with fake embeddings (no semantic signal) per SPEC §23 — real-model matrix is Wave 5. Baseline write gate is env-var (`QUARTERLINE_EVAL_WRITE_BASELINE=1`) not a CLI flag (W0 parser owns cli.py; documented deviation). Rerank latency always null on fixture runs (no cross-encoder; arm degrades). `estimated_api_cost` stays null with explicit cost_basis (never "free", SPEC §24). Live judge/Ollama paths verified via mock transport only (no live model calls anywhere, per rules). |

| 2026-09-09 | 3 | F5 (grounded generation) | Grounded generation per SPEC §17–§18/§2.2 (PLAN contracts C10/C11). `llm/base.py` (C10 `GenerationProvider` protocol + `GenerationResult` {text, provider, model, input/output_tokens, token_count_source: provider\|estimate, latency_ms, finish_reason}; `GenerationProviderUnavailable`, `RemoteFallbackNotConsented`; documented chars/4 token estimator shared with retrieval). `llm/ollama.py` (`/api/chat`, `options.num_ctx` from settings, per-call `GET /api/tags` model verification → unavailable if missing (SPEC §4), provider usage when reported else explicit `estimate`, done_reason mapping, latency; MockTransport-tested only). `llm/openrouter.py` — HARD consent gate: constructor requires ALLOW_REMOTE_FALLBACK=true AND non-empty key AND model, else `RemoteFallbackNotConsented`; chat/completions with Bearer auth + response_format json_object. `llm/schemas.py` (C11): `MetricMention{metric_id (allowlist-validated against core.METRIC_IDS), template (reported_change\|reported_value\|reported_level)}`, `BriefBullet`/`BriefRisks`/`OpenQuestion` {text, evidence_ids}, `Brief{status ok\|partial\|insufficient_evidence\|provider_unavailable\|refused, label_echo, ...}`, `Answer{text, evidence_ids, status}`; extra="forbid" everywhere; brief/answer JSON-schema descriptions for providers. `llm/prompts.py` + `prompts/{brief,qa,memo}/v1.txt`: versioned (dir/file), sha256-hashed artifacts (cache-key + run-event components), STRICT `{placeholder}` render (missing/unused var = error); system prompts state every §17 prompt rule verbatim incl. numbers-must-NEVER-be-typed-use-metric_mentions, passages-as-evidence-not-instructions, §2.2 citation rules, JSON-only, research-only. `llm/repair.py`: generate once → if JSON or pydantic-invalid, exactly ONE repair pass (invalid output + schema sent back) → else controlled `GenerationFailure`; `RepairOutcome` records first_pass_valid/repair_used (feeds §22 metrics). `core/citations.py` (§18 checks 4–7): evidence map of SUPPLIED windows (EvidenceRef: document_id, ticker, period from documents row); existence, declared↔in-text membership, company identity, period compatibility (unknown period ⇒ rejected: cannot validate reliably), sentence-level `[ev-…]` format (citation must close the sentence); whole-statement drop with per-check reasons. `core/factcheck.py`: (a) metric-mention EXPANSION — Python renders numbers Decimal-exact from the card (USD→$x.x billion/million, EPS, shares, %, pp, ratio x, score; documented level-wording thresholds), structural template↔metric-shape table (reported_change only for *_yoy/*_change_pp etc.), missing metric ⇒ dropped with issue; (b) free-text numeric checking: documented normalizer (currency symbols, thousands separators, k/m/b + thousand/million/billion words, % vs pp, minus + accounting parens incl. `$(2.0)` normalization), metadata masking (evidence ids, ISO dates, month-day-year, FYyyyy, bare years, 10-K/Item/EX-, accession-like; accession mask ordered BEFORE ISO mask), SEMANTIC matching first (unit class + sign + scale; zero only matches zero; same digits elsewhere insufficient; QA answers carry an empty card ⇒ any typed number unsupported), ≤0.5% relative tolerance only AFTER semantic match plus display-rounding respect (1/2-dp displays). `core/advice_policy.py` (`advice-policy-v1`, no LLM): advice-SEEKING request detector (buy/sell/hold near investment context, should-I-invest, good/best stock, priced to buy, price target, worth buying, undervalued, recommend holding…) and advice-ISSUING content detector (you-should, we-recommend, is-a-buy, price target of, guaranteed returns); research-only refusal text with alternatives + §12.1 caption. `llm/generation.py`: pipeline = fact card (label) → SearchService (mode presets, company + period filters, hybrid w/ lexical fallback on embedding-model mismatch per §25) → bounded context (≤6 windows) → insufficient-evidence policy ABSTAINS without provider call → brief_cache lookup keyed sha256(company, period, fact-card hash excl. generated_at, evidence/context hash, provider, model, prompt version+hash, generation settings, validation/factcheck/advice versions) → ONE provider call ([system, user JSON payload: allowlist + metric catalog (id/unit/status, no values) + evidence]) → repair → VALIDATION GATE `validate_generation` checks 1–10 in order (schema, status/abstention, label_echo EQUALITY vs code label, citations 4–7, metric-reference validity, numeric consistency, advice compliance) with `ValidationReport` (per-check reasons, retained/dropped counts, version `validation-v1`); survival: drops ⇒ partial, nothing useful ⇒ insufficient_evidence, advice-driven loss ⇒ refused, provider down ⇒ provider_unavailable (facts+evidence shown, never fake prose), invalid-after-repair ⇒ controlled failure; mention expansion rendered BEFORE assembly; cache persist successes-only w/ expiry; C12 events (prompt version/hash, tokens+source, latencies, json_valid (+first_pass), repair_used, citation_valid, factcheck_passed, retained/dropped bullets, answer_status, cache_hit, error_type, estimated_api_cost=null). `answer_question`: advice check FIRST (refusal without provider call), no-ticker ⇒ explicit company-scope notice, citation+number+advice gated. API: `POST /api/c/{ticker}/brief` (JSON or urlencoded body {period_end?}; HTMX/Accept → `partials/brief.html`, else JSON; 400 unknown fields/bad date, 404 unknown ticker/period) and `POST /api/ask` (JSON {question, ticker?, mode}, pydantic extra-forbid; partial `partials/ask.html`); LLM imports are LAZY inside handlers so deterministic routes never import the stack (F4 block_llm_modules tests stay green); explicit degraded banner "generation unavailable — showing facts and evidence, not generated prose". `memo_review.html` static shell (wave-4 header). main.py: only the F5 marker line replaced (+ import line); F6 marker untouched. Tests: 87 new (`tests/generation_test_helpers.py` seeds facts fixture + period-ALIGNED document corpus + section index offline; ScriptedProvider records calls; unit: schemas / prompts (§17 rules verbatim) / providers (MockTransport: tags-miss, request shape + num_ctx + format, estimate-vs-provider, consent-gate matrix) / repair (2-call guarantee, fenced JSON, controlled failure) / citations / factcheck (18: normalization incl. `$(2.0)`, k/m/b, pp-vs-%, metadata masking, semantic + tolerance + sign + zero, Decimal-exact expansion) / advice / validation gate (§26 generation list incl. invented + wrong-period citations, label mismatch, unsupported 44.7%, structural wrong-metric, partial survival, refusal); integration: pipeline (ok brief w/ rendered numbers + resolvable citations + misaligned-doc exclusion, single repair e2e w/ event assertions, cache hit zero provider calls, provider-down degradation, abstention zero calls, ask refusal / insufficient / grounded) + API (degraded JSON + HTML banner, advice refusal, 404/400s, GET 405, success partial via injected provider). | `uv run pytest -q` → 496 passed (309 before; includes F6 concurrent additions); `uv run ruff check .` → All checks passed; `ruff format` applied to my 24 files only (project-wide format left to orchestrator due to concurrent waves); TestClient verification (real output): POST /api/c/AAPL/brief w/ Ollama dead → 200 {status: provider_unavailable, 26 fact rows, 2 evidence windows, brief: null, reason "ollama unreachable…"}; POST /api/ask advice → 200 {status: refused, advice-policy-v1, research-only text}; HX brief → banner "generation unavailable … not generated prose" present; `quarterline eval generation` (F6 harness) runs over my pipeline exit 0. | Limitations/unverified: no live Ollama/model calls anywhere (offline rule) — provider paths verified via httpx.MockTransport only; free-text numeric matching cannot verify CONCEPT identity from prose (documented; the metric_mentions channel is the designed answer), so unit-class+sign+scale+tolerance is the enforced contract; wrong-period citation gate enforced at the evidence-map level while retrieval's period filter already excludes misaligned documents in normal operation (gate proven with synthetic + misaligned-fixture evidence); open-question premise validation is limited to citation/number/advice checks (semantic premise check would need the judge wave); brief cache has no eviction beyond BRIEF_CACHE_HOURS read-time expiry check; my events land in the C12 JSONL stub until F6's runs/run_events backend (signatures unchanged); `partials/brief.html` is exposed for company.html integration but company.html placeholder panel NOT edited (F4's file) — orchestrator wires the include/HTMX post into `POST /api/c/{ticker}/brief`. |
| 2026-09-09 | 3 (orchestrator) | ZCode | Fixed `_pack_chunks` early-stop bug (document tail abandoned when a chunk trimmed inside its predecessor) → lexical Hit@5 71→100%, section/hybrid 4/7→6/7; regenerated fixture embeddings (60 chunk vectors, new index versions) and eval baseline via explicit `QUARTERLINE_EVAL_WRITE_BASELINE=1` gate (diff: 7 configs improved; fixed/dense 57→43% recorded as fake-embedding artifact, not semantic signal); wired brief panel into company.html (HTMX → POST /api/c/{ticker}/brief); dashboard nav activated; CLI `--mode/--top-k` flags added; frozen eval test levels updated to measured values | 496 passed, ruff clean | Real-model eval (nomic/qwen) still pending Wave 5; brief panel untested against live model |
| 2026-09-10 | 4 | F7 (agent workflow) | Bounded agent memo workflow per SPEC §20. `agent/` package: LangGraph `StateGraph` workflow (`begin` entry router → validate_request → classify_intent → create_plan → execute_read_tools → write_memo → validate_memo → await_export_approval → export) with a rule-based planner (allowlist `READ_TOOLS` only), node wrapper enforcing budgets between nodes (each read tool once; ≤4 tool calls incl. export; ≤12 transitions counted separately; one memo repair pass via `llm.repair`; injectable-clock deadline checked between nodes) and checkpointing the FULL state to `agent_runs.state_json` after every transition (committed per node = true crash boundary); resume/re-entry routed deterministically (no tool ever re-called). Four typed tools in `agent/tools.py` (`get_company_facts` via `build_fact_card` — no SQL surface; `search_filings` via SearchService with lexical fallback on embedding mismatch; `get_prices` graceful `price_unavailable` — never blocks a facts-only memo; `export_memo` approval-gated, server-generated filename `memo-{run_id}.{md|json}` under `STORAGE_DIR/exports`, run-id regex rejects all path components); every attempt (incl. rejections) audited to `agent_tool_calls` before/after execution with args sha256 + duration. `agent/approval.py`: approvals bound to run_id + SHA-256 of exact memo content + export type + expiry; content change/expiry/type mismatch → invalid; re-verified inside the export handler. `agent/schemas.py`: MemoRequest/MemoOutput/MemoDraft (extra=forbid, METRIC_IDS allowlist, 5 fixed headings) + AgentRunResult with budgets + tool_call_log + trace_uri. Memo gate reuses F5 checks (citations 4–7, metric 8, numeric 9 via factcheck, advice 10; label equality; metric mentions Python-rendered). `YfinancePriceProvider` implemented (lazy `yfinance` import; errors → `PriceProviderUnavailable`; tests use fakes/stubbed modules — no network). `store/repositories/runs.py` (RunsRepo: upsert_run/state, tool-call audit rows, approvals). `api/routers/memo.py`: POST /api/memos (synchronous run), GET /api/memos/{run_id} (JSON or memo_review.html), POST /api/memos/{run_id}/approve-export (409 on unapproved/expired/tampered/type-mismatch); main.py wave-4 marker line; memo_review.html shell populated + partials/memo.html (autoescaped; only `[ev-…]` tokens become /evidence/ links); `docs/failure_cases.md` with 7 REAL exercised failures + traces. Tests: `tests/unit/test_agent_{tools,approval,schemas}.py`, `test_yfinance_provider.py`, `tests/integration/test_agent_graph.py`, `test_memo_api.py`, shared `tests/agent_test_helpers.py`. Verification: `uv run pytest -q` → 561 passed (496 + 65 new); `uv run ruff check .` clean, `ruff format .` applied; TestClient run: POST /api/memos → awaiting_approval (budgets 2/4 tools, 8/12 transitions), approve-export with tampered memo → 409 ("memo content changed"), valid → `memo-{run_id}.md` created under tmp STORAGE_DIR/exports. | 561 passed, ruff clean | No AGENT_DEADLINE_SECONDS setting in config (F0-owned) — deadline is a workflow parameter (60 s default); `agent_tool_calls` lacks duration/args-hash columns (recorded inside result_json/arguments_json, exposed in AgentRunResult); live yfinance/Ollama paths unverified offline by design (PLAN rule 8); memo runs are synchronous MVP (SPEC §20) — approval/export happens via a second request; injection defense is structural (deterministic planner + citation gate + escaping), no semantic injection detector (documented limitation in failure_cases.md) |
| 2026-09-10 | 5 | F8 (integration + portfolio docs) | Final build wave. **PostgreSQL 16 + pgvector profile (SPEC §15/§29)**: `store/repositories/search_postgres.py` — `PostgresSearchIndexRepo` subclasses `SearchIndexRepo` and overrides only backend-specific pieces (raw DDL at `ensure_schema`: `CREATE EXTENSION vector`, `chunk_fts` tsvector+GIN lexical table with ON DELETE CASCADE, `chunk_vectors` pgvector table mirroring `embeddings` identity columns, PG-typed `index_manifest`; inherited portable methods: idempotent `write_chunks`, C8 `get_chunk_by_evidence_id`, `windows_for_children`, `evidence_rows`, manifest reads, corpus hash/stats; overrides: tsvector `sync_fts`, pgvector `store_embedding`/`get_cached_vectors`/`get_vectors_for_chunks`, `record_manifest` upsert). `search_lexical_postgres` = `to_tsquery('english')` + `ts_rank_cd` + GIN, **labeled `postgres-tsrank` everywhere user-visible (never "bm25"), score higher-is-better documented as opposite of FTS5 sign**; `search_dense_postgres` = metadata-filtered SQL + native `<=>` cosine distance (score=1−distance), strict (provider, model, revision) identity filter, dim-guard raises ValueError like `vector.py`; `ensure_embedding_model_match` raises `EmbeddingModelMismatchError` (§15.7); opt-in `create_vector_index` (IVFFlat, per §15 "indexes AFTER correctness tests", not called by default); `sanitize_tsquery_query` tokenizes to `[A-Za-z0-9_]{2,}` joined with `|` (operators inert); `format_pg_vector`/`parse_pg_vector`. Factory `get_search_repo(session, settings)` selects by `DATABASE_URL` prefix (postgres* → PG repo). `docker-compose.yml`: `pgvector/pgvector:pg16`, service `quarterline-db`, loopback-only 5432, named volume, healthcheck, LOCAL-DEV-ONLY credentials documented. `Makefile test-postgres`: `docker compose up -d quarterline-db` (tolerates Docker-absent with a notice), pg_isready wait, `QUARTERLINE_TEST_POSTGRES_URL=… pytest tests/integration/test_postgres_profile.py`, `docker compose stop` — idempotent (drop/create schema per test; volume persists). `tests/integration/test_postgres_profile.py`: `pytestmark = pytest.mark.postgres` + 6 contract cases mirroring the SQLite suite (rebuild idempotency + C8 evidence roundtrip + window expansion; pgvector roundtrip + native cosine ordering + strict model identity; dim-mismatch rejection; tsrank lexical + metadata filters + hostile-query sanitization; manifest index-version derivation + model-mismatch gate + §15.6 new-version-per-model; corpus stats + backend labels) with layered skips: module-level skip when no Postgres DBAPI (psycopg/psycopg2/pg8000 tried), session-fixture skip on fast <1 s TCP probe failure / connection error / missing `vector` extension — **skip, never fail, on every default run incl. CI** (`-m 'not live'` addopts untouched). **Docs set** per SPEC §28–§31 honesty standards: `architecture.md` (§5 diagram, module map, data flow, §25 degradation matrix + PG row, contracts C1–C12), `data_contract.md` (all §9 tables + repo-owned auxiliary tables, decimal-as-TEXT, unique keys, lineage roles, C8 formula, index-version formula), `financial_methodology.md` (tagmap order, period bands, YTD/Q4 conditions + never-derived list, ratio/growth/capex-sign rules, label precedence, score weights/rescaling/60% floor — each with SPEC section + proving test file), `retrieval_experiments.md` (re-measured fixture matrix via `eval retrieval` on an isolated temp DB: fixed/lexical 100.0/100.0/MRR 0.929, fixed/dense 42.9, fixed/hybrid 85.7/0.619, section/lexical 100.0/1.000, section/dense 85.7/0.445, section/hybrid 85.7/0.714, rerank arms = degraded duplicates, wrong-co 0%, period-err 0%, n=11; labeled "fixture corpus, deterministic fake embeddings, NOT real-model measurement"; chunker-fix history 71.4→100 / 4-of-7→6-of-7 + gated baseline regen incl. fixed/dense 57.1→42.9 fake-embedding-artifact note; Real-model section marked PENDING), `evaluation.md` (dataset schema, 11 committed questions table, Hit@5-vs-Recall@5 distinction, generation metrics, judge policy, 5pp thresholds + explicit-regeneration rule, CI §23 honesty), `threat_model.md` (12-row STRIDE-lite incl. injection→failure_cases.md §7, typed-tools-only, export path traversal, consent gate, HTML isolation, secrets, SEC rate compliance + explicit non-goals), `commercial_readiness.md` (all §28 items as NOT-done + "what this release is NOT"), `README.md` full rewrite (honest quickstart with real-EDGAR_IDENTITY gate stated, §29 commands, offline/Ollama/unverified status table, §31 acceptance met/partial/pending table, docs index, portfolio framing). | `uv run pytest -q` → **561 passed, 1 skipped** (the pgvector module, skip-by-design), 2 warnings (pre-existing starlette TestClient DeprecationWarning + PytestUnknownMarkWarning for the unregistered `postgres` marker — pyproject is wave-0-owned; one-line fix: add `"postgres: PostgreSQL/pgvector contract tests (skipped without server+driver)"` to markers). `uv run ruff check` + `ruff format` clean on all F8 files. `docker compose config` → exit 0 (validated; services NOT started per instructions). `make` itself absent from this Git Bash (documented Makefile caveat) so `test-postgres` recipe verified by inspection; its pytest portion exercised directly. Fixture eval re-run command isolated: `DATABASE_URL=sqlite:///<tmp> EMBED_PROVIDER=fake uv run python -m quarterline eval retrieval --dataset data/eval/questions.jsonl` → matrix matches committed baseline, regression PASS | **PG CONTRACT TESTS NEVER EXECUTED AGAINST A LIVE SERVER**: no Postgres DBAPI is installed (psycopg/psycopg2/pg8000 all missing) and adding one requires editing orchestrator-owned `pyproject.toml` — the profile is code-complete + compose-ready but criterion §31.10 stays PARTIAL; enabling = add `psycopg` (or `pg8000`) to dev deps, then `make test-postgres`. WIRING GAP for orchestrator: `get_search_repo()` exists but `retrieve/search.py` (lines ~192/354) still constructs `SearchIndexRepo` directly — PG profile activates end-to-end only after those call sites route through the factory (all files involved are outside my scope). Marker `postgres` unregistered (warning only; `--strict-markers` not set). Docker daemon not exercised: `docker compose up` deliberately not run per instructions, so the compose image pull + `make test-postgres` happy path remain unexercised. Project-wide `ruff check .` currently reports 5 errors + 2 format-needs in files NOT owned by F8 (orchestrator's concurrent `scripts/demo_real_models.py`, and pre-existing `tests/unit/test_ratios.py` formatting) — left untouched per ground rule 2. Fixture eval must run against an isolated `DATABASE_URL` temp store because the dev store's index manifest now names `ollama/nomic-embed-text` (orchestrator's concurrent real-model build) and the §15.7 mismatch gate correctly refuses fake-provider queries against it |
| 2026-09-09 | 5 (orchestrator + F8) | ZCode + F8 | Wave 5: PG16+pgvector profile (PostgresSearchIndexRepo via get_search_repo factory, docker-compose, psycopg extra, 6 contract tests skip-without-server), 7-doc set + README, provenance growth-lineage fix in core/ratios.py, brief-prompt v2, schema-constrained Ollama decoding (metric_id/label_echo/evidence_ids enums), failures-excluded-from-brief-cache fix, GENERATION_TIMEOUT_SECONDS setting. Real-model verification (scripts/demo_real_models.py, fixture-seeded store): nomic-embed-text index+hybrid search PASS (768-dim, real 8-K exhibit evidence); qwen3:4b reaches the gate with schema-valid JSON, correct label echo, real citation IDs — residual 4B failures (inline [ev-] format, template-pair validity, occasional empty echo on qwen2.5:7b) all caught by §18 checks → honest insufficient_evidence, never unvalidated prose | 561 passed + 6 PG skips, ruff clean | PG contract tests never run against a live server (Docker daemon down); no clean ok/partial brief from local 4B/7B models yet — needs a stronger instruct model or UI-level handling; live SEC ingestion still gated on EDGAR_IDENTITY |
| 2026-09-10 | 5b (orchestrator) | ZCode | Docker daemon started; PG profile exercised against live pgvector/pg16 container for the first time: fixed column-name mismatch (`dim`→`dimension`) in dim-mismatch predicates, corrected one unverified test expectation (row-family counts) — all 6 pgvector contract tests now pass live; real-model 2×4 retrieval matrix measured with nomic-embed-text (7/8 configs at 100% Hit@5, section/lexical MRR 1.0, zero wrong-company, p95 <60ms) and published in docs/retrieval_experiments.md; added demo_script.md + cv_bullets.md (M9) | 567 passed (incl. 6 live-PG), ruff clean | Brief `ok` path from local ≤7B models still unproven (guardrail path measured instead); live SEC ingestion gated on EDGAR_IDENTITY |
| 2026-09-11 | D-IND1 | IND-1 (India source feasibility: acquisition) | Phase D first task per SPEC §27: NSE/BSE source feasibility for INFY + HINDUNILVR. Identifiers verified from 4 official sources each (NSE quote page, BSE stock page, company IR, and inside NSE XBRL instances: INFY=INE009A01021/500209, HINDUNILVR=INE030A01027/500696). Access findings: plain httpx rejected everywhere (infosys.com+hul.co.in 403 Akamai incl. direct PDFs; bseindia.com 403; nseindia.com+nsearchives silent TCP drop, CORS-restricted) — all 26 documents acquired via real browser session only, no anti-bot bypass, no aggregators; no rate-limit responses at ~30 req/hour. Key structural findings: NSE legacy financial-results module frozen at Dec-2024 quarter; from Mar-2025 quarter everything is under Integrated Filing- Financials (undocumented `/api/integrated-filing-results`, works in browser session; per-filing scope Consolidated/Standalone + audited status + revision fields + XBRL/iXBRL URLs); instances use SEBI `in-capmkt` Integrated Finance Ind AS taxonomy IFIndAs V2.1 (26-06-2026), schemaRef relative (schema not bundled), values full rupees with LevelOfRounding=Crores. Cached 26 files (sha256 in `tests/fixtures/india/manifest.json`): 16 company-IR PDFs/XLSX (results, condensed FS, press releases, presentations, transcripts, HUL SEBI-format workbooks) + 10 exchange XBRL/iXBRL (consolidated+standalone × 2 periods × 2 companies). Committed 4 byte-identical XBRL fixtures (<5 MB, unexcerpted). Cash-flow frequency (design rule): exchange quarterly instances have ZERO cash-flow facts; annual indirect CF only in Q4 filings (INFY CFO ₹33,986 Cr FY26, HUL ₹10,999 Cr FY26, full-year context verified); HUL has no quarterly CF anywhere (PDF+workbook checked); Infosys quarterly CF exists only in IR condensed-FS PDFs ("Three months ended June 30", CFO ₹9,330 Cr Q1FY27) → app must never fabricate quarterly CF for India | manifests/audit valid JSON+structure; hashes re-verified with sha256sum after every move | Automated headless-browser fetch unverified from project runtime; NSE API endpoints undocumented (fragile); BSE download path unassessed; revision handling untested (Original-only sample); HUL FY26 discontinued ops (ice-cream demerger) needs explicit handling; audit: docs/india_source_audit.md |
| 2026-09-11 | Phase D (IND-2) | IND-2 (India parsing + reconciliation) | Built `src/quarterline/sources/india/` (14 modules, all new): stdlib-ElementTree `xbrl_parse` for SEBI in-capmkt IFIndAs instances (contexts incl. xbrldi dimensions, units INR + INRPerShare, decimals, `LevelOfRounding` trait, qualifier facts, per-instance taxonomy version); `units` (LAKH/CRORE constants, full-rupee passthrough — `LevelOfRounding` never applied as multiplier, EPS/per-share never scaled, display-string parser with explicit declared scale or `AmbiguousScaleError`); `periods` (FY 1 Apr–31 Mar; quarter/YTD/annual/instant classified from dates only; Q1=30 Jun … Q4=31 Mar; prior-year-quarter helper); `concept_map` + `data/tagmap_india.yml` (10 India-only canonical concepts from OBSERVED tags; US concept IDs rejected at load; uncertain fallbacks flagged; unknown tags explicit-unmapped); `revisions` (`FilingMeta` carried from exchange listing, `select_latest` latest-publication-wins + as_of filter); `issuers` (typed registry, verified-only guard); `pdf_results` (thin scale/scope header detection, ambiguous → `review_required`, no LLM); `nse`/`bse` documented endpoint stubs raising `ManualImportRequired` (no runtime network); `ir_documents` (manual import: verify → sha256 → content-addressed cache under `storage/raw/india/<slug>/<qN_fy…>/` → `source_artifacts` row → company upsert country IN / INR, cik column carries BSE scrip code — 12-char ISIN exceeds String(10); `.filing.json` metadata sidecar; scope cross-check vs `NatureOfReportStandaloneConsolidated`); `pipeline` (`ingest_observations`: in-capmkt observations into `fact_observations` via `FactsRepo` insert-or-skip, observation_hash = US identity pattern with scope in frame slot, rounding trait + scope + revision + audit in `context_metadata_json`, source_fy/source_fp from dates; re-run adds 0 rows); `reconcile` (source-linked table + `data/india_reconciliation.csv`, 78 rows from fixture parses, all `reconciled_to_source`); CLI handlers `ingest:india-document` + `verify:india` registered at package import. Granted edit: added `cache_file()` to `ingest/cache.py` (existing functions untouched). FINDINGS: taxonomy version varies per filing (Q4 FY26 instances carry `IFIndAs V2.0 (06-02-2026)`, Q1 FY27 carry `V2.1 (26-06-2026)` — refines IND-1 manifest note); scope is declared per-instance by `NatureOfReportStandaloneConsolidated`, NOT an XBRL dimension; revision field exists ONLY in the exchange listing, not the instance; HUL Q1 declared `Unaudited` vs INFY `Audited`; INFY Q1 carries an undimensioned `SegmentRevenue` total on the quarter context (left unmapped, reported); no 6M/9M or prior-year duration contexts in these instances. `scripts/india_ind2_verification.py` reproduces the INFY end-to-end check on a scratch store (39 observations; second run 0). Docs: audit §9 IND-2 findings; new `docs/india_financial_methodology.md` (units, scope, periods, annual-only cash-flow rule incl. never-annual-as-Q4 + H2 only from compatible annual−H1, revision policy, no-EBITDA-conflation). | `uv run pytest -q` → 693 passed, 6 skipped (132 new India tests: units/periods/concept_map/xbrl_parse-on-real-fixtures/issuers/revisions/pdf_results/ir_documents/pipeline-idempotency/reconcile); `uv run ruff check .` + `ruff format .` clean | CLI subcommands unreachable from `python -m quarterline`: `quarterline.sources.india` is NOT in `cli._WAVE_PACKAGES` and `cli.build_parser()` has no `ingest india-document` / `verify india` subparsers (argparse rejects `verify india: invalid choice`) — handlers verified via `SUBCOMMAND_REGISTRY` directly; orchestrator to add both. No revised filings existed to test against (synthetic-only). Full table extraction from IR PDFs, BSE downloads, and browser-grade fetching remain later milestones |
| 2026-09-11 | D-feas (orchestrator) | ZCode | Wired India CLI (`ingest india-document` with full provenance flags, `verify india`); fixed argparse dest + content-hash artifact dedup on re-import (re-imports reuse the existing source_artifacts row and preserve seq metadata); live end-to-end on dev store: both INFY exchange XBRLs imported (171 + 526 facts), idempotent re-import, source-linked reconciliation table reproduces official values (Q1 FY27 revenue ₹48,211 Cr / PAT ₹7,775 Cr / EPS 19.19; FY26 revenue ₹1,78,650 Cr, exceptional -₹1,289 Cr, annual CFO ₹33,986 Cr — no quarterly CF in exchange data, as IND-1 found) | 693 passed + 6 skips, ruff clean | Feasibility milestone acceptance met: parser distinguishes quarter vs cumulative, consolidated vs standalone (per-instance, cross-checked), scales (Crores trait, EPS exempt), original vs revised (carried; all current filings Original) |

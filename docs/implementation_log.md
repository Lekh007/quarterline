# Implementation Log

Append-only. Each wave records: completed work, commands run, test results, known limitations, unverified integrations (SPEC §0 rule 7).

| Date | Wave | Agent | Summary | Tests | Unverified / Limitations |
|---|---|---|---|---|---|
| 2026-09-09 | 0 | F0 (foundation) | Installable `src`-layout package (hatchling, py3.12, uv.lock). pydantic-settings `Settings` (all SPEC §7 fields; placeholder-identity gate; SEC interval clamped up to ≥200 ms). SQLAlchemy 2 ORM for all 19 SPEC §9 tables (decimals as canonical TEXT via `decimal_to_text`/`text_to_decimal`; pinned unique keys on `fact_observations.observation_hash`, `normalized_facts` period identity, `chunks`, `embeddings`). Alembic initial migration `2679d5a12c9a_initial_schema` + programmatic `quarterline db upgrade`. `SecClient`: identity gate, thread-safe monotonic ≥200 ms request spacing (undershoot-proof loop), 429/503/conn-error backoff up to `SEC_MAX_RETRIES`, disk cache w/ ETag/Last-Modified revalidation + dated-cache fallback (SPEC §25). `fetch_companyfacts`/`iter_unit_facts`, `fetch_submissions`/`list_recent_filings`/`build_filing_url`, ordered `tagmap_us.yml` loader. FastAPI shell: `/` placeholder + footer disclaimer, security headers (nosniff, CSP self+inline), `/health` (DB SELECT 1; Ollama providers ≤0.5 s timeout, 30 s cache; 503 only on DB fail). `SUBCOMMAND_REGISTRY` CLI (db upgrade + serve live; others exit 2 with wave notices). Events stub (C12, JSONL). Watchlist CIKs verified against official `https://www.sec.gov/files/company_tickers.json` (script kept at `scripts/verify_watchlist_ciks.py`; that file 403'd once transiently — script has a data.sec.gov/submissions fallback that also verifies ticker+name). | `uv sync --extra dev` ok; `uv run alembic upgrade head` ok (stamps `alembic_version`, idempotent); `uv run pytest -q` → 45 passed (offline: MockTransport SEC tests, tmp-file migration tests, TestClient health tests, decimal/uniqueness ORM tests); `uv run ruff check .` + `uv run ruff format --check .` clean; `/health` via TestClient → 200 `{'status': 'ok', 'database': 'ok', 'generation_provider': 'ok', 'embedding_provider': 'ok'}` (Ollama running locally); `GET /` → 200 with disclaimer + nosniff/CSP headers; `quarterline ingest facts` → exit 2 wave-1 notice | Live SEC ingestion unverified (EDGAR_IDENTITY still placeholder — app refuses by design). `sentence-transformers` declared in `rerank` extra, not installed. PG profile, retrieval, generation, agent = later waves. SQLite treats NULL `period_start` as distinct in the `normalized_facts` unique key (instant facts); repos must upsert by full key. Alembic env.py must not run PRAGMAs on its connection (pysqlite rolls back the version stamp otherwise). AMZN = Consumer Discretionary (GICS), which the task brief's sector list omitted. |
| 2026-09-09 | 1 | F2 (documents) | Document pipeline per SPEC §13: `ingest/html_clean.py` (`html_clean-1`: content validation, EDGAR SGML `<DOCUMENT>` wrapper stripping, script/style/nav/hidden removal — scripts never executed or retained, inline XBRL `ix:*` unwraps to text with `ix:header` dropped, tables → readable `cell \| cell` rows, whitespace normalization, exact per-block `start_offset`/`end_offset` into cleaned text with roundtrip invariant); `ingest/sections.py` (`SectionCandidate{section_type,heading,start,end,confidence,notes}`; 10-Q Part I Item 2 / Part II Item 1A, 10-K Item 7 (never 7A) / Item 1A; ToC avoidance = largest-after-heading-body match wins, item/part state machine, title-keyword confirmation; uncertainty demoted to `other` @0.2 with note, short-body @0.5, missing sections stay missing — never a whole-filing mislabel; `earnings_release_candidate` = whole-exhibit section with signal score); `ingest/pdf_extract.py` (`pymupdf-1`: per-page offsets consistent in joined text, `ok/needs_ocr/failed`, <25 extractable chars → `needs_ocr`, corrupt → `failed` with hash retained); `store/repositories/documents.py` (`DocumentsRepo`: doc identity = company+accession+kind+source_url (exhibit filename), content-hash idempotency lookup, destructive `replace_sections`, offset/span resolution for later evidence spans, extraction-report counts; namespace-package import verified); `ingest/documents.py` (`documents-1`: watchlist discovery 4×10-Q + 2×10-K + 8-K Item 2.02 via submissions `items` array + `index.json` directory EX-99 name match (index files excluded), SecClient cache-first download → `source_artifacts` provenance (sha256, etag, last_modified, local_path, parser_version) → clean → sections → `documents`+`sections`; 8-K three-date logic `resolve_8k_dates` (filing date, event date = period-of-report, discussed period parsed ONLY from exhibit text "quarter/months/year ended <date>", else null + note — never inferred from filing date); `ingest_pdf_file` for IR PDFs (URL/company/date/page/hashes retained; needs_ocr → zero sections); `IngestReport.summary()`; `handle_ingest_documents` registered into `SUBCOMMAND_REGISTRY["ingest:documents"]` at import — no cli.py edit). Prices stub `sources/prices/{__init__,yfinance_provider}.py` (`YfinancePriceProvider.get_prices` raises NotImplementedError; wave F7 activates). Fixtures committed under `tests/fixtures/documents/` with `manifest.json`: REAL Apple 8-K EX-99.1 Q2 FY2026 (accession 0000320193-26-000011, 168,815 bytes, unmodified, sha256 on record) + synthetic `injection_filing.html` (labeled, stored as inert verbatim text) + synthetic PyMuPDF `synthetic_mda.pdf`; manifest records fetch UA + the Archives-403 adaptation (added contact suffix to the plan UA after www.sec.gov rejected the bare form; data.sec.gov accepted it). 41 tests (html_clean 12, sections 13, pdf_extract 8, repo 5, pipeline e2e 7: end-to-end tmp-DB, idempotent re-run no duplicates, 3-date fabricated-payload unit, CLI handler via `cli.main`, injection stored inert, PDF provenance, needs_ocr not indexed). | `uv run pytest -q tests/unit/test_html_clean.py tests/unit/test_sections.py tests/unit/test_pdf_extract.py tests/unit/test_documents_repo.py tests/integration/test_documents_pipeline.py` → 41 passed; full suite excluding F1's concurrently in-flight files → 86 passed; `uv run ruff check` (my scope) clean; `ruff format` applied to my 12 files only. Pipeline-over-fixtures into tmp DB printed: 3 documents (10-Q ok mda[169,469)+risk_factors[500,739); real 8-K exhibit ok earnings_release[0,11342) period_end=2026-03-28 ≠ filed 2026-04-30; injection exhibit ok, discussed 2026-06-30) + text PDF ok (page section, 1047 chars) + image-only scan → needs_ocr, 0 sections | Unverified/limitations: live SEC discovery+download through `SecClient` unexercised end-to-end (identity gate; EDGAR_IDENTITY still placeholder) — pipeline verified offline via fixture-backed fake transport; `quarterline ingest documents` standalone still prints the W0 wave notice because nothing in the console-script path imports `quarterline.ingest.documents` (handler IS registered; needs a one-line import in `ingest/__init__.py` or orchestrator-owned `ingest/pipeline.py` — both outside my file list); W0 `documents` schema has no event_date/notes column → 8-K event date + determination note ride on `IngestReport.eight_k_dates`, `period_end` carries the discussed period (recommend migration); W0 `sections` schema has no confidence/notes columns → carried on `SectionCandidate`, surfaced in `IngestReport.low_confidence_sections`; documents table has no DB unique constraint on the identity key — enforced in `DocumentsRepo.upsert_document` only; F1's concurrent test files (`test_normalization/test_periods/test_quarter_label/test_ratios/test_quality/test_facts_*`) were failing/erroring during my runs (their churn also trips W0 `test_cli_events::test_unimplemented_subcommands_exit_2_with_notice` once `ingest:facts` registers — orchestrator will need to update that W0 test at wave boundaries); project-wide `ruff format .`/`ruff check .` gated by F1 files, not mine |
| 2026-09-09 | 1 | F1 (financial) | Financial normalization end-to-end (SPEC §10–§12, Milestone 2). `core/periods.py`: duration-band period-kind inference (13/14-week quarters, 52/53-week years; no 90-day assumption), FYE-month fiscal math with 53-week spill handling, fiscal anchors, quarter-sequence helpers, `QuarterPeriod`. `core/normalization.py`: companyfacts→observations (raw fy/fp kept in `context_metadata_json`, sha256 `observation_hash` over company+tag+unit+periods+value+frame → re-ingestion idempotent, revised values preserved alongside originals); selection groups by economic-period identity then applies tag priority AFTER unit/scope filtering (annual can never displace quarterly); YTD→quarter derivation (Q2=6m−Q1, Q3=9m−6m, Q4=annual−9m) with all §10.3 conditions incl. contiguity + revision-vintage window (vintage attaches to the value's FIRST filing, so unchanged comparatives don't manufacture mismatches); EPS/weighted-shares/instants never derived; missing concepts logged never filled; `as_of` policy excludes filings after timestamp. `core/ratios.py`: all §11 ratios in Decimal via MetricValue {value,unit,status,input refs,formula_version}; capex preserved-then-normalized sign semantics w/ unexpected-sign review flags (no blind abs()); growth null for non-positive priors + turned-profitable/loss-making notes; margin changes in pp; cfo_to_net_income `unsuitable` when NI≤0. `core/quarter_label.py`: §12.1 five-signal label with exact precedence (2-quarter cfo_ni<0.4 veto first, <3 available → insufficient data, ≥3 true/false, else mixed), every input+rule exposed; §12.2 experimental fundamental score with scale/clamp, weight rescaling, no aggregate below 60% weight. `core/quality.py`: per-quarter coverage (direct/derived/missing fraction). `core/provenance.py`: `build_fact_card(s)` (latest 8 quarters + latest annual) with observation-id lineage or derived-from + formula_version on every MetricValue; `explain_metric` ProvenanceReport (accession/form/filed_at, direct vs derived vs computed, formula text). `store/repositories/{base,companies,facts}.py`: typed session-scoped repos; hash insert-or-skip observations; NULL-`period_start`-safe full-key upserts (SQLite NULL-in-unique-key gotcha); lineage roles direct_source/annual_total/prior_ytd; idempotent derived-metric upserts; `quarters_available`/`facts_for_quarter`/`metric_series`/as-of variants (as-of reconstructs direct + derived facts from observations/lineage at the timestamp). `ingest/facts.py`: `ingest_facts` pipeline (watchlist → SecClient cache-first fetch → observations → normalize → derive → derived metrics; per-company error isolation; idempotent re-run) + `verify facts` table; CLI handlers registered via `register_subcommand` at module import. Fixtures: real trimmed AAPL companyfacts (fetch 2026-09-09, UA "Quarterline build (fixture verification)", tagmap tags only, period end ≥ 2023-06-01, 400 verbatim entries, sha256 in manifest) + clearly-marked synthetic edge-case fixture (Oct FYE, 53-week year, restatement, unit-incompatible tag, EPS-never-derived) + provenance manifest. Tests: 147 new (83 unit + 64 integration incl. full AAPL-fixture pipeline, restatement as_of, idempotency, CLI) — all §26 financial+score lists covered. | `uv run pytest -q` → 192 passed (45 W0 + 147 new); `uv run ruff check .` + `uv run ruff format --check .` clean; `verify facts` through SUBCOMMAND_REGISTRY exit 0 with per-concept 8-quarter provenance table; re-ingestion second run: 0 observations inserted, 0 facts created; demo: AAPL latest-4-quarter revenue/net_income/cfo all reconcile with fixture values, Q4s derived from annual−9m with lineage. | CLI WIRING GAP: `quarterline/cli.py` imports nothing from ingest (by W0 design, file not owned by F1) so `uv run python -m quarterline ingest facts / verify facts` still exits 2 until one line is added to `src/quarterline/ingest/__init__.py`: `import quarterline.ingest.facts  # noqa: F401`. Handlers themselves are verified through the registry. Conservative revision-vintage window (≤100 days between input filings) can refuse derivations where inputs are filed >1 quarter apart; W0 wave-notice unit test (`tests/unit/test_cli_events.py::test_unimplemented_subcommands_exit_2_with_notice`) still expects `ingest facts` unimplemented — my integration tests guard/clean the registry so the full suite stays green, but the orchestrator may want to update that W0 assertion at the wave boundary. Live SEC ingestion still unverified (EDGAR_IDENTITY placeholder); fixture fetch used the declared build UA. |
| 2026-09-09 | 2 | F4 (UI/API deterministic) | All SPEC §19 non-LLM routes, deterministic over the store, zero LLM imports. `routers/companies.py`: watchlist `GET /` (15 rows from the DB, AAPL fixture metrics + "no data ingested" graceful rows with no zero substitution, HTMX partial swap on `HX-Request`), company page `GET /c/{ticker}` (?period_end quarter picker, 8-quarter Chart.js charts fed by an inline `#chart-data` JSON payload + always-rendered summary table as the no-JS fallback, per-quarter fact table with direct/derived(subtraction)/derived(formula)/missing badges, FORMULA_TEXT + formula_version per row, warnings column, SPEC §12.1 label panel showing all five signals (id/input/state/detail), the applied rule and the exact caption, brief placeholder panel "available in a later release"), provenance page + JSON `GET /c/{ticker}/provenance/{metric_id}?period_end=` (METRIC_IDS allowlist gate -> 404; C6 `explain_metric`; display-level enrichment restores growth-metric lineage because core `derived_from` refs are `revenue_yoy:current`-style and unresolvable by `explain_metric` — upstream fix belongs in core, see limitations). JSON APIs: `/api/c/{ticker}/facts[?period_end=]` (fact card), `/api/c/{ticker}/metrics` (8 quarters); all numbers serialized via pydantic `model_dump(mode="json")` (Decimal -> canonical string, never float); unknown ticker/period/metric -> 404 `{"error": ...}`. Decimal-exact display helpers ("n/a"/"missing", never "0") exported for reuse. `routers/screener.py`: exact filter set `revenue_yoy_gt`/`operating_margin_gt`/`operating_margin_change_pp_gt`/`fcf_positive`/`quarter_label`/`sector`/`minimum_data_coverage`, GET + POST (urlencoded body parsed directly — python-multipart not a dependency), every submitted value echoed, explicit empty state, HTMX partial support, invalid decimals -> visible per-field errors with the filter dropped (never coerced). `routers/filings.py`: filing viewer `GET /filings/{document_id}` (metadata incl. the 8-K dates + hardened rel=noopener-noreferrer sec.gov link; ONLY cleaned `sections` text, Jinja autoescape on, never the raw `\|safe`-style injection of filing HTML per SPEC §2.3.7) and `GET /evidence/{evidence_id}` per contract C8 (sha1 over document_id+strategy+start+end+text_hash resolved against `chunks`; explicit "evidence not found (index not built)" 404 until wave F3 populates it; finalized at integration). `main.py`: router registration only (placeholder `/` moved into `routers.companies`; CSP untouched). `base.html`: nav Watchlist/Screener/Dashboard (dashboard 404s gracefully until wave F6) + research-only + label disclaimer footer on every page; vendored htmx 1.9.12 (0-Clause BSD) + Chart.js 4.4.3 (MIT) downloaded into `static/vendor/` with `LICENSES.md` provenance record (page works fully offline; if Chart.js fails to load, `app.js` degrades to the server-rendered tables). `static/app.css` (tables, badges, chips, form grid) + `static/app.js` (charts from the Decimal-string JSON payload; Number conversion only for plotting). Tests: 45 new — `tests/api/test_api_json.py` (8-quarter series, recursive no-float assert over the JSON, Q4 missing EPS, 404 JSON matrix, provenance contract, LLM-modules-blocked via sys.modules=None) and `tests/integration/test_ui_{watchlist,company,screener,filings}.py` (15-row watchlist + 14 graceful empty rows + no-zero-substitution, HTMX partial, disclaimer on every page parametrized, security headers, label panel signals, missing-badge rows, quarter picker, hand-derived screener expectations read from the fixture (YoY = Q3-2026 rev / Q3-2025 rev − 1), combined/label/sector/coverage filters + validation errors + POST echo, escaped <script> and injection-fixture text rendered inert, evidence C8 resolve + not-built 404), with `tests/ui_test_helpers.py` (reuses `facts_test_helpers` fixture pipeline; `facts_cli_guard` keeps the W0 CLI-registry test green) and `scripts/ui_smoke.py` (boot check). | `uv run pytest -q tests/api tests/integration/test_ui_watchlist.py tests/integration/test_ui_company.py tests/integration/test_ui_screener.py tests/integration/test_ui_filings.py` -> 45 passed. `uv run pytest -q` -> 236 passed, 1 failed (pre-existing wave conflict, not F4: `tests/unit/test_cli_events.py::test_unimplemented_subcommands_exit_2_with_notice` now executes wave-F3's real `index:build` handler via the cli `_WAVE_PACKAGES` lazy import — the same wave-boundary pattern F1 logged; F3's files also transiently broke `ruff check` mid-edit). `uv run ruff check` on the F4 scope (src/quarterline/api, scripts/ui_smoke.py, my test files) -> clean; `ruff format` applied to my 10 py files only. `uv run python scripts/ui_smoke.py` -> fixture ingest errors: none; 21 routes exercised with OLLAMA at a dead port: / 200, /screener 200 (GET + POST + filters), /c/AAPL 200 (+period_end variant), /c/MSFT 200 (graceful empty), /c/NOPE 404, provenance page+JSON 200, bogus metric 404, facts/metrics JSON 200, unknown ticker 404, /filings/{id} 200, /filings/999999 404, /evidence/{resolved-id} 200, /evidence/ev-0123456789ab 404 (index not built), /health 200; smoke: OK. | Unverified / limitations: (1) CORE GAP for orchestrator/F1: `core/provenance.explain_metric` cannot resolve input lineage for growth-style metrics (`revenue_yoy`, `shares_yoy`, `operating_margin_change_pp`) because their `derived_from` refs are `<metric_id>:current` / `:prior_year_quarter` rather than concept names, so `input_concepts` parses to the metric's own name and `sources` returns empty; F4 works around it with a display-level enrichment map (`routers/companies.py::_enrich_growth_sources`, mirrors FORMULA_TEXT) — the authoritative fix belongs in core, after which the enrichment stays a no-op (it fires only when sources are empty). (2) `/evidence/{id}` scans `chunks` linearly and is finalized at integration with F3 (chunks were empty in my tests; the seeded-chunk contract test proves the C8 id scheme). (3) `/dashboard` nav link 404s gracefully until wave F6. (4) Screener POST parses urlencoded bodies directly because python-multipart is not a project dependency; multipart form posts are unsupported (browser default and HTMX are urlencoded). (5) Invalid screener input -> visible error with that filter dropped (never silently coerced). (6) Live ingestion still gated on EDGAR_IDENTITY (unchanged); the UI is verified only over committed fixtures. |
| 2026-09-09 | 2 | F3 (retrieval) | Retrieval track per SPEC §14–§16 (SQLite profile, PLAN C7/C8/C9). `retrieve/chunk_fixed.py` (`fixed-1`: ~400-token windows, ~80-token overlap, paragraph-boundary preference via blank-line blocks, oversized paragraphs split at sentence bounds then word-window hard cap ≤460; single documented estimator `estimate_tokens` = ceil(chars/4), labeled approximate; shared block/packing engine with guaranteed-progress overlap backtracking + fully-contained-span suppression); `retrieve/chunk_section.py` (`section-1`: children ~200 tok/40 overlap, parent = full section stored, query-time expansion pre-materialized as bounded ≤560-token `window` rows so every supplied window stays C8-resolvable without truncation — 560 chosen ≤ §16's 600-token passage cap; task's ~1200 example deviated, documented). Row-family convention: `strategy_version` suffixes `-parent`/`-window`; only base rows embedded/FTS-indexed/retrievable; identical-text drafts skipped (tiny sections collapse to one child). `retrieve/embeddings.py` (C9 protocol; `OllamaEmbeddingProvider` batched `/api/embed`, availability check via `/api/tags`, Nomic `search_document:`/`search_query:` prefixes applied by callers, normalize flag, dim learned on first embed; `FakeEmbeddingProvider` deterministic hash vectors, NON-PRODUCTION, `EMBED_PROVIDER=fake` for offline CLI/tests; `EmbeddingProviderUnavailable`, `EmbeddingModelMismatchError`). `store/repositories/search_sqlite.py` (`SearchIndexRepo`: idempotent chunk writes via unique key (rebuild ⇒ identical rows/ids), contentful FTS5 `chunks_fts(text, chunk_id UNINDEXED)` kept in sync per document, float32-LE vector BLOBs, embeddings cache lookup by (text_hash, provider, model, revision) reused ACROSS strategies, C8 `get_chunk_by_evidence_id` (content-derived scan; persisted evidence_id column would need a migration — reported), child→window map, `index_manifest` table (raw-SQL schema outside W0 ORM, migration recommended): deterministic `index_version` = sha256(provider|model|revision|strategy|strategy_version|corpus_text_hash_set)[:16], per-(strategy,version,model) upsert; corpus stats). `retrieve/lexical.py` (FTS5 joined to metadata filters, one fixed-fragment arm per strategy UNION ALL'd, expanding lists as explicit per-item bound params, `ORDER BY bm25() ASC` with documented more-negative-better sign + rank exposure; `sanitize_fts_query` quotes every word token — operators/NEAR/colons/punctuation dropped, injection-safe). `retrieve/vector.py` (metadata filter FIRST in SQL, then NumPy cosine over BLOB vectors, dim guard, deterministic ties). `retrieve/fusion.py` (RRF Σ1/(60+rank), ranks only, deterministic tie-break, top 15). `retrieve/reranker.py` (`CrossEncoderReranker` import-guards `sentence_transformers` ([rerank] extra, not installed), lazy load w/ cached failure reason, `maybe_rerank` never raises, never fakes scores). `retrieve/search.py` (`SearchService` §16 flow: mode section presets brief={mda,earnings_release} risk={risk_factors,mda} general=all, explicit sections override mode; lexical 20 + dense 20 → RRF 15 → optional rerank → top_k; hybrid degrades to lexical on provider unavailability (§25); model-mismatch gate raises `EmbeddingModelMismatchError` for dense/hybrid when the strategy's manifest was built with a different model; `evidence-policy-v1` deterministic abstention (≥2 items hybrid / rank≤5 lexical / cosine≥0.20 dense anchor + ≥1 query-term overlap — explicitly NOT calibrated probabilities); `build_index` per-section chunking with offset shifting into document coordinates; run events via C12 stub; `index:build`+`search` CLI handlers registered at package import, `EMBED_PROVIDER=fake` supported offline). `retrieve/context.py` (≤6 passages, ≤600 tok each, 10% estimator headroom on the budget, same-document overlap dedup, second-source diversity for passage 2, oversized windows skipped whole — never truncated so evidence ids stay resolvable; budget = MAX_PROMPT_TOKENS−MAX_OUTPUT_TOKENS−headroom). Fixtures: `tests/fixtures/retrieval/{generate_fixtures.py,fixture_embeddings.json}` — 17 chunk + 5 query vectors from FakeEmbeddingProvider(seed 20260909, dim 64) over the offline fixture corpus (real 8-K + synthetic 10-Q/injection/PDF), regeneration byte-identical (sha256-verified twice), manifest documents provenance ("NOT real model output — deterministic test artifact") + regen command; `tests/retrieval_test_helpers.py` corpus fixture. Tests: 57 new (43 unit + 14 integration incl. CLI) covering the §26 retrieval list verbatim: correct-company (lexical+dense+hybrid), correct-period (disjoint docs per discussed period), form filter, FTS5 ordering w/ documented sign, RRF recomputed from source ranks (+hand-computed unit case), embedding-model mismatch rejection (lexical survives), bounded parent expansion (≤560), context budget, evidence-ID C8 roundtrip + offset-slice invariant vs document text, reranker failure fallback (degraded metadata, no fake scores), rebuild idempotence (identical chunk ids) + strategy coexistence (fixed/section/parent/window families), mode presets + explicit override, insufficient evidence (empty corpus/no-match) + sufficient pass, index_version changes with model, fixture-vs-provider vector regression. Wave-boundary update (same as orchestrator's wave-1 edit): `tests/unit/test_cli_events.py` wave-notice assertions switched from `index build`/`search` (now implemented, handlers registered from `quarterline.retrieve`) to the still-stubbed wave-3 `eval` subcommands. | `uv run pytest -q` → 309 passed (192 before + 117 new incl. retrieval); `uv run ruff check .` → All checks passed; `ruff format --check .` → 108 files already formatted (formatted only my 17 files); `uv run python -m quarterline index build --strategy fixed --help` → usage, exit 0; `search --help` shows --ticker/--query/--strategy/--retrieval; e2e demo over the REAL fixture corpus: ingest (3 SEC docs + PDF) → `index build` fixed (11 chunks, 11 emb, version 7d91c9b81fb2797f) + section (6 children+3 parents/10 windows, version 783028b11161d7e1) → `search --ticker AAPL --strategy section --retrieval hybrid "Apple revenue March quarter results"` ranks the real EX-99.1 window ev-0f0259cc182a #1 with lexical+dense+rrf scores; hybrid-rerank degrades with `degraded[reranker]` and keeps fused order; unknown ticker → `INSUFFICIENT EVIDENCE (evidence-policy-v1)`; run events JSONL per search with corpus_version/latency/status | Limitations/unverified: Ollama embedding path verified only via httpx.MockTransport request-shape tests (Ollama not exercised live — `nomic-embed-text` not pulled; live path is code-complete); PG/pgvector profile is wave F8 scope; W0 `chunks` schema has no role/evidence-id column → row-family encoded via `strategy_version` suffix and evidence ids resolved by content-derived scan (both documented, migration recommended); full cleaned document text is not persisted by the W0 pipeline, so the index covers extracted sections only (un-sectioned interstitial text is not searchable — indexer chunks per section; acceptable because sections carry the evidence, reported for orchestrator awareness); W0 search parser has no `--mode`/`--top-k`/`--tickers` flags → handler reads `getattr(args,...)`/`QUARTERLINE_SEARCH_MODE` env (gap for orchestrator's parser, one line each); F4's concurrent API/UI files were excluded from my runs — project `ruff check .` was clean at my final run; query-embedding caching not implemented (query vectors recomputed per search — chunk cache makes indexing cheap; queries have no chunk row to key on). |
